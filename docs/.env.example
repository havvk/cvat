# 这是一个 CVAT 环境的配置示例文件。
# 在使用时，请将此文件复制到项目根目录，并重命名为 .env，然后填入您的实际值。

# --- CVAT 核心服务配置 ---

# CVAT_HOST 是用于访问 CVAT 的主要主机名 (域名或IP地址)。
# 这个值会被用来构建回调URL等，非常重要。
# 示例: CVAT_HOST=cvat.example.com
CVAT_HOST=your_primary_hostname_or_ip

# ALLOWED_HOSTS 是一个用逗号分隔的白名单列表，包含所有允许访问此 CVAT 实例的主机名。
# 建议至少包含您的主要主机名和 localhost。
# 示例: ALLOWED_HOSTS=cvat.example.com,192.168.1.10,localhost
ALLOWED_HOSTS=your_primary_hostname_or_ip,localhost

# --- UI 插件配置 ---

# 需要在 CVAT UI 中加载的客户端插件列表 (用空格分隔)。
# 如需启用 SAM2 插件，请取消下面这行的注释。
# CLIENT_PLUGINS=plugins/sam2

# --- Docker Compose 特定配置 ---

# 您可以为 Docker Compose 定义一个项目名称。
# COMPOSE_PROJECT_NAME=cvat

# --- 代理配置 (可选) ---

# 如果您的服务器需要通过代理访问互联网 (例如，在构建或拉取模型时),
# 可以在此指定。这些变量会被传递到相关的服务容器中。
# HTTP_PROXY=http://your-proxy-address:port
# HTTPS_PROXY=http://your-proxy-address:port
# NO_PROXY=localhost,127.0.0.1
